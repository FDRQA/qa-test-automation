class LoginPage {
    constructor(page) {
        this.page = page;
        this.emailInput = 'input[type="email"]';
        this.passwordInput = 'input[type="password"]';
        this.loginButton = 'button:has-text("Iniciar sesión")';
        this.openAppButton = 'button:has-text("Open the desktop app")';
    }

    async navigate() {
        console.log("🚀 Abriendo página de login...");
        await this.page.goto('https://drive.internxt.com/login');
    }

    async login(email, password) {
        console.log("📌 Ingresando credenciales...");
        await this.page.fill(this.emailInput, email);
        await this.page.fill(this.passwordInput, password);
        await this.page.click(this.loginButton);
    }

    async confirmLoginSuccess() {
        console.log("✅ Verificando login...");
        await this.page.waitForURL('https://drive.internxt.com/auth-success');
    }

    async openDesktopApp() {
        console.log("🔵 Haciendo clic en 'Open the desktop app'...");
        await this.page.click(this.openAppButton);
    }
}

module.exports = { LoginPage };  // ✅ Eliminamos la coma innecesaria